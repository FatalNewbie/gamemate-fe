import React from 'react';
import { Box, Typography } from '@mui/material';

const Chat = () => {
    return (
        <Box>
            <Typography variant="h4">홈페이지</Typography>
        </Box>
    );
};

export default Chat;
